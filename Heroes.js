function teamCap() {
    var heroe = "Capitan America"; 
    //variable heroe con el valor Capitan America
    console.log(heroe); 
    // Se imprime el valor de heroe 
}

teamCap(); // Se llama a la función teamCap, lo que ejecuta el console.log y muestra Capitan America

function teamIronMan() {
    var heroe = "Iron Man"; 
    // variable heroe con el valor "Iron Man"
    console.log(heroe); 
    // Se imprime el valor de heroe 
}

teamIronMan(); // Se llama a la función teamIronMan, lo que ejecuta el console.log y muestra Capitan America
